import { DisplayTemperaturePipe } from './display-temperature.pipe';

describe('DiplayTemperaturePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayTemperaturePipe();
    expect(pipe).toBeTruthy();
  });
});
